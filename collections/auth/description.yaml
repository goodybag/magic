---
resource:
  type: item
  path: /session
methods:
  get:
    desc: Get Session
  post:
    desc: Start Session
    body:
      email:    { type: email required, eg: user@goodybag.com }
      password: { type: string required, eg: password }
  delete:
    desc: End Session
...