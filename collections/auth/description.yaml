---
resource:
  type: item
  path: /session
methods:
  get:
    desc: Get Session
  post:
    desc: Start Session
    body:
      email:    { type: any, eg: user@goodybag.com, required: true }
      password: { type: any, eg: password, required: true }
  delete:
    desc: End Session
...